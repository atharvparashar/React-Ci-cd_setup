name: "sample react ci pipeline /workflow"

on:
   push:
     branches:
       - main
jobs:
   build-and-test:
     runs-on: ubuntu-latest


     steps:
       - name:  "setup node js"
         uses: actions/setup-node@v4
         with:
           node-version: '21'